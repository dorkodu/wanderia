export function createGoal() {

  /** 
   * If a duplicate ULID exists (extremely rare), the server returns an error 
   * (e.g., HTTP 409 Conflict) to the client. 
   * The client then generates a new ULID and retries the sync for that entity.
  */
}

export function getGoal() { }

export function updateGoal() { }